@Library('todoshop') _

todoshop (
        COMPONENT             : 'login',
        PROJECT_NAME          : "todoshop",
        SLAVE_LABEL           : "GOLANG",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "GOLANG"
)